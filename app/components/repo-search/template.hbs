<PowerSelect
  @searchEnabled={{true}}
  @search={{fn this.searchRepo}}
  @selected={{this.selected}}
  @onChange={{fn this.setSelected}} as |repo|>
  {{repo.full_name}}
</PowerSelect>
(It might fail if the API limit is exceeded)